# Systemd and Services

## Used commands

* `systemctl`
* `journalctl`
* `systemd-analyze`

# Tasks

## 1. Start the `sshd` service.

```bash
# Place for future command
```

## 2. Configure the `sshd` service to start automatically at system boot.

```bash
# Place for future command
```

## 3. Check why the `httpd` service is not running.

```bash
# Place for future command
```

## 4. Display all `systemd` units that failed during the last system boot.

```bash
# Place for future command
```

## 5. Display the dependencies of the `sshd.service` unit.

```bash
# Place for future command
```

## 6. Start a service once without enabling it to start automatically at boot.

```bash
# Place for future command
```

## 7. Display the system boot time and the startup time of individual services.

```bash
# Place for future command
```

## 8. Display the last 20 log entries from the `sshd` service.

```bash
# Place for future command
```

