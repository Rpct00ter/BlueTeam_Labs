# 15. Shell Environment
## Used commands

* `alias`
* `export`
* `env`
* `printenv`
* `grep`
* `/etc/profile`
* `~/.bashrc`
* `history`

# Tasks

## 1. Create an alias `ll` available only to the current user.

```bash
# Place for future command
```

## 2. Add the `/opt/scripts` directory to the `PATH` environment variable for the current session.

```bash
# Place for future command
```

## 3. Check the current environment variables, especially the `EDITOR` variable.

```bash
# Place for future command
```

## 4. Configure a persistent `EDITOR` environment variable set to `kate` (or `vim`) for all users on the system.

```bash
# Place for future command
```

## 5. Display command history entries containing the word `docker`.

```bash
# Place for future command
```

I also corrected the numbering (`2.5` → `3`, etc.) and added `env`, `printenv`, `grep`, and `history`, since they are directly useful for these tasks.
