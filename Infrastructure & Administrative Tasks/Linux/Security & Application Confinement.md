# Linux Security & Application Confinement
## Used commands

* `aa-status`
* `aa-enforce`
* `aa-complain`
* `aa-disable`
* `firejail`
* `firejail --list`
* `getenforce`

## Tasks
### AppArmor
---
### 1. Check whether AppArmor is enabled and display its current status.

```bash
# Place for future command


### 2. Display the AppArmor profiles currently loaded on the system.

```bash
# Place for future command
```

### 3. Check the AppArmor status of a selected application and determine whether its profile is running in **Enforce** or **Complain** mode.

```bash
# Place for future command
```


### Firejail
---
### 4. Run a selected application inside a Firejail sandbox.

```bash
# Place for future command
```


### 5. Display all applications currently running inside Firejail sandboxes.

```bash
# Place for future command
```

### 6. Run an application inside Firejail with network access disabled.

```bash
# Place for future command
```


### SELinux
---
### 7. Check whether SELinux is installed and determine whether it is running in **Enforcing**, **Permissive**, or **Disabled** mode.

```bash
# Place for future command
```
